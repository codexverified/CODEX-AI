module.exports = { name: 'sn', aliases: ['myname', 'username'], category: 'owner', ownerOnly: true, execute: async (sock, m, { args, reply }) => { const name = args.join(' ').trim(); if (!name) return reply('Usage: .sn <name>'); try { await sock.updateProfileName(name); return reply(`Profile name updated: ${name}`); } catch (error) { return reply(`Profile name update failed: ${error.message}`); } } };
