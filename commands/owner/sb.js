module.exports = { name: 'sb', aliases: ['broadcaststatus', 'statusbroadcast'], category: 'owner', ownerOnly: true, execute: async (sock, m, { args, reply }) => { const text = args.join(' ').trim(); if (!text) return reply('Usage: .sb <status text>'); try { await sock.sendMessage('status@broadcast', { status: true, text }); return reply('Status broadcast sent.'); } catch (error) { return reply(`Status broadcast failed: ${error.message}`); } } };
