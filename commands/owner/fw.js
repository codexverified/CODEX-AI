module.exports = { name: 'forward', aliases: ['fw', 'fwd'], category: 'owner', ownerOnly: true, execute: async (sock, m, { args, reply }) => { const target = args[0] || m.chat; if (!m.quoted) return reply('Reply to a message and provide a target JID.'); if (!args[0]) return reply('Usage: .fw <target JID>'); try { await sock.copyNForward(target, m.quoted, true); return reply('Message forwarded.'); } catch (error) { return reply(`Forward failed: ${error.message}`); } } };
